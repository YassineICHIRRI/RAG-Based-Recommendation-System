from setuptools import setup, find_packages

setup(
    name='src',
    version='0.0.1',
    packages=find_packages(),
    install_requires=[
        ""
    ],
    # Metadata
    author='',
    author_email='',
    description="",
    url='',
)